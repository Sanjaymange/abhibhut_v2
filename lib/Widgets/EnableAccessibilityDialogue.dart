import 'package:flutter/material.dart';

class EnableAccessibilityDialogue extends StatelessWidget {
  const EnableAccessibilityDialogue({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
